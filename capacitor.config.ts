import type { CapacitorConfig } from '@capacitor/cli';

const config: CapacitorConfig = {
  appId: 'com.example.app',
  appName: 'CalculadoraCientificaApp',
  webDir: 'www'
};

export default config;
